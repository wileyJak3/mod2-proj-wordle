// THis context contains the api data recieved after take the word provide by the user and searching for them within the apis

import { createContext } from 'react';

 const ApiContext = createContext([])

export default ApiContext;